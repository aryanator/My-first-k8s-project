apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: flask-app
  template:
    metadata:
      labels:
        app: flask-app
    spec:
      containers:
      - name: flask-container
        image: flask-k8s:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 5000
        env:
        - name: APP_MESSAGE
          valueFrom: 
            configMapKeyRef:
              name: flask-config
              key: APP_MESSAGE

    
      
    
